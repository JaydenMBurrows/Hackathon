// generate music beams 
// import MusicBeam from './MusicBeam.js';
export default class MusicBeamController {
    // class variables for musicBeamController    

    constructor(canvas) {
        this.canvas = canvas;
    }

    // blast function (shoot beam)
            // add beam to beam array
            // new MusicBeam()

        // draw function
            // for each beam, update position and fillRect(x, y, w, h)
}